#include<iostream>
#include<cmath>
using namespace std;
inline float sqrtroot(float n){
    float sqrt=pow(n,0.5);
return sqrt;
}
int main(){
    float n;
        cout<<"enter the value of n:";
    cin>>n;
    cout<<"square root of a number is :" << sqrt(n);

    return 0;
}

